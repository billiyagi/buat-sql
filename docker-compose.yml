services:
  sqldiagram:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    environment:
      - NODE_ENV=production
      - DATABASE_FILE=data/sqlite.db
    restart: unless-stopped
